# Copy to .env.local to enable real Gemini synthesis in the Investigation panel.
# Without this, ECHO falls back to a deterministic template built from the same
# mock-data tool outputs, so the app runs fully offline by default.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
